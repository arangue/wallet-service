# Application
PORT=8081

# Database (matches docker-compose defaults)
DATABASE_URL=postgres://user:password@localhost:5434/wallet_db?sslmode=disable

# Integration tests — point to a running Postgres instance
# Tests are skipped automatically if this is not set
TEST_DATABASE_URL=postgres://user:password@localhost:5434/wallet_db?sslmode=disable
